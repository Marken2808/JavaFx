enum Gender {
    MALE, FEMALE, OTHER
}

enum Title {
    Mr, Mrs, Miss
}



entity Name {
	title Title required,
    firstName String required,
    middleName String,
    lastName String required,
    displayName String required,
}

entity Address {
	numberic String,
    street String required,
    county String,
    city String,
    postcode String,
    country String,
}

entity Customer {
    email String required,
    phone String required,
    birth String required,
    gender Gender,
}

/* Property */
enum PropertyStatus {
	Sold,
    Selling,
    ToSell,
    Let,
    Letting,
    ToLet
}

enum PropertyType {
	Accommodation,
    Project,
    Land
}

entity Property {
	title String required,
	type PropertyType required,
    status PropertyStatus required,
    isUrgent Boolean,
}

enum AccommodationType {
	Flat,
	House,
    Studio,
    Ensuite,
    Apartment
}

enum AccommodationStatus {
	Furnished,
    Unfurnished,
    Partfurnished
}

entity Accommodation {
	title String required,
    type AccommodationType required,
    status AccommodationStatus required,
    total Double,
}

enum RoomType {
	Single_room,
	Double_room,
    King_room
}

entity Room {
	title String required,
    type RoomType required,
    acreage Double required,
    image ImageBlob,
    price Double,
}

enum AreaType {
	Attic,
	Lounge,
    Garage,
    Basement,
    Lavatory,
    Bedroom,
    Diningroom,
    Kitchen,
    Balcony,
    Landing,
    Hall
}

entity Area {
	title String required,
    type AreaType required,
}

relationship OneToMany {
    Property to Customer{property(title)},
    Area to Room{area(type) required},
}

relationship ManyToOne {
    Customer{name(displayName) required} to Name,

}

relationship OneToOne {
    Customer{user(login) required} to User,
    Property{address(street) required} to Address,

}

relationship ManyToMany {
	Accommodation{room} to Room{accommodation}

}


